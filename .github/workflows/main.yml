name: Docker Compose 기반 Spring Boot 앱 배포

on:
  push:
    branches:
      - prod

permissions:
  contents: read

jobs:
  build-and-push:
    name: Docker 이미지 빌드 및 푸시
    runs-on: ubuntu-latest

    steps:
      - name: GitHub 레포지토리 체크아웃
        uses: actions/checkout@v4

      - name: JDK 17 설정
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle Wrapper 실행 권한 부여
        run: chmod +x ./gradlew

      - name: Gradle 빌드 (테스트 제외)
        run: ./gradlew build -x test
        env:
          DB_HOST: localhost
          DB_PORT: 3306
          DB_NAME: test_db
          DB_USERNAME: test
          DB_PASSWORD: test
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          OAUTH2_CLIENT_NAME: test_client
          OAUTH2_GOOGLE_CLIENT_ID: test_client_id
          OAUTH2_GOOGLE_CLIENT_SECRET: test_client_secret
          OAUTH2_GOOGLE_REDIRECT_URI: http://localhost:8080/login/oauth2/code/google

      - name: DockerHub 로그인
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: Docker 이미지 빌드 및 푸시
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/swyp11-6-backend:latest

  deploy-to-server:
    name: 서버에 Docker Compose로 배포
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: GitHub 레포지토리 체크아웃 (배포용)
        uses: actions/checkout@v4

      - name: 서버 배포 (이미지 업데이트 및 재시작)
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            cd /root

            echo "🔧 .env 파일 생성/업데이트"
            cat > .env << EOF
            DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
            DB_HOST=localhost
            DB_PORT=3306
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            JWT_EXPIRATION=${{ secrets.JWT_EXPIRATION }}
            OAUTH2_CLIENT_NAME=${{ secrets.OAUTH2_CLIENT_NAME }}
            OAUTH2_GOOGLE_CLIENT_ID=${{ secrets.OAUTH2_GOOGLE_CLIENT_ID }}
            OAUTH2_GOOGLE_CLIENT_SECRET=${{ secrets.OAUTH2_GOOGLE_CLIENT_SECRET }}
            OAUTH2_GOOGLE_REDIRECT_URI=${{ secrets.OAUTH2_GOOGLE_REDIRECT_URI }}
            EOF

            echo "📥 최신 이미지 다운로드"
            docker pull ${{ secrets.DOCKER_USERNAME }}/swyp11-6-backend:latest

            echo "🔄 백엔드 컨테이너만 재시작"
            docker compose up -d --no-deps --build backend

            echo "🗑️ 사용하지 않는 이미지 정리"
            docker image prune -f || true

            echo "⏳ 서비스 준비 대기 (20초)"
            sleep 20

            echo "🔍 서비스 상태 확인"
            docker compose ps

            echo "✅ 배포 완료! 백엔드 서비스 로그:"
            docker compose logs --tail=50 backend